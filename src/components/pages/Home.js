const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)
	

	return (
		<>
			<h2>Home Page</h2>
			{/* <header/> */}

			{/* <sidebar/> */}
			{/* ^this will contain a map of categories 
			(map videos and return its category part of the schema*/}
			
			{/* <post/> */}
			{/* ^this will contain the video,comment, comment submission props */}
		</>
	)
}

export default Home
